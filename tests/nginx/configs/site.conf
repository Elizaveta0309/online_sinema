upstream api-test {
    server api-test:8000;
}

server {
    listen 80;

    location ~ ^\/(api)\/ {
        proxy_pass http://api-test;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
